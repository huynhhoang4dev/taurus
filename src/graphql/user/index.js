import {
    GraphQLObjectType,
    GraphQLSchema,
    GraphQLFieldConfigMap,
    GraphQLInterfaceType,
    GraphQLFieldResolver
} from 'graphql'

export default new GraphQLSchema({
    query:
    mutation:
})